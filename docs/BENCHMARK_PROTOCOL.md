# Benchmark Protocol

Every scenario should record the commit SHA, configuration, seed, attack/fault window, model mismatch, detection thresholds, true physical degradation state, detection delay, false alarms, diagnosis class, source trust, quality impact, and production-state occupancy.
