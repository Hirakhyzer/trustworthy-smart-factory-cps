# Reproducibility

For every reported figure or benchmark preserve the Git commit, Python/dependency versions, config, random seed, command, summary JSON, timeseries CSV, and generated figures. Synthetic results must be labeled as simulation output.
